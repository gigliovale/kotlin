// "Replace ',' with '||' in when-condition" "true"
fun test(i: Int, j: Int) {
    var b = false
    when {
        <caret>i > 0 || j > 0 -> { /* some code */ }
        else -> { /* other code */ }
    }
}